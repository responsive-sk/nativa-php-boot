import { EventName } from '@lit/react';
import { AgCheckbox, CheckboxProps, CheckboxChangeEvent } from '../core/Checkbox';
import * as React from 'react';
/**
 * React-specific props interface derived from core CheckboxProps
 * Extends core props including event handlers
 */
export interface ReactCheckboxProps extends CheckboxProps {
    children?: React.ReactNode;
    className?: string;
    id?: string;
    onClick?: (event: MouseEvent) => void;
    onChange?: (event: CheckboxChangeEvent) => void;
}
export declare const ReactCheckbox: import('@lit/react').ReactWebComponent<AgCheckbox, {
    onChange: EventName<CheckboxChangeEvent>;
}>;
export type { CheckboxChangeEvent, CheckboxSize, CheckboxTheme, } from '../core/Checkbox';
//# sourceMappingURL=ReactCheckbox.d.ts.map