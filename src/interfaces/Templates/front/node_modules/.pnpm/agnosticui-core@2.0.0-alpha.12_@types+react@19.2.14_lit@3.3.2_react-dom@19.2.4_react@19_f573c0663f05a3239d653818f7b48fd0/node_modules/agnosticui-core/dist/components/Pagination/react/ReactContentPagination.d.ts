import { EventName } from '@lit/react';
import { ContentPagination, ContentPaginationProps, NavigateEvent } from '../core/ContentPagination';
import * as React from 'react';
/**
 * React-specific props interface for ContentPagination component
 * Extends core ContentPaginationProps with React-specific additions
 */
export interface ReactContentPaginationProps extends ContentPaginationProps {
    children?: React.ReactNode;
    className?: string;
    id?: string;
}
export declare const ReactContentPagination: import('@lit/react').ReactWebComponent<ContentPagination, {
    onNavigate: EventName<NavigateEvent>;
}>;
export type { NavigateEvent, NavigateEventDetail, ContentNavigationItem, } from '../core/ContentPagination';
//# sourceMappingURL=ReactContentPagination.d.ts.map