import { LitElement as e, css as r, html as s } from "lit";
const a = class a extends e {
  render() {
    return s`
      <div class="avatar-group" part="ag-avatar-group">
        <slot></slot>
      </div>
    `;
  }
};
a.styles = r`
    :host {
      display: inline-flex;
    }

    .avatar-group {
      display: flex;
      flex-direction: row;
      align-items: center;
    }

    /* Overlap avatars except the first one */
    ::slotted(ag-avatar:not(:first-child)) {
      margin-inline-start: calc(-1 * 0.625rem);
    }

    /* Optional: Add z-index stacking so later avatars appear on top when hovered */
    ::slotted(ag-avatar) {
      transition: transform 0.2s ease;
      position: relative;
    }

    ::slotted(ag-avatar:hover) {
      transform: translateY(-2px);
      z-index: 10;
    }
  `;
let t = a;
customElements.get("ag-avatar-group") || customElements.define("ag-avatar-group", t);
export {
  t as AvatarGroup
};
