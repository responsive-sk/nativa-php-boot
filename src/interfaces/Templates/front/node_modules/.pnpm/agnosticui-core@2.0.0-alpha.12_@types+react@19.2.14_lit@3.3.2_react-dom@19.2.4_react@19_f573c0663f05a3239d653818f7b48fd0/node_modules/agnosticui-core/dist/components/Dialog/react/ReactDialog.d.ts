import { EventName } from '@lit/react';
import { AgnosticDialog, DialogProps, DialogOpenEvent, DialogCloseEvent, DialogCancelEvent } from '../core/Dialog';
import * as React from 'react';
/**
 * React-specific props for Dialog
 */
export interface ReactDialogProps extends DialogProps {
    children?: React.ReactNode;
    className?: string;
    id?: string;
}
/**
 * Helper component for dialog header slot
 */
export interface DialogHeaderProps {
    children?: React.ReactNode;
}
/**
 * Helper component for dialog footer slot
 */
export interface DialogFooterProps {
    children?: React.ReactNode;
}
export declare const ReactDialog: import('@lit/react').ReactWebComponent<AgnosticDialog, {
    onDialogOpen: EventName<DialogOpenEvent>;
    onDialogClose: EventName<DialogCloseEvent>;
    onDialogCancel: EventName<DialogCancelEvent>;
}>;
export declare const DialogHeader: React.FC<DialogHeaderProps>;
export declare const DialogFooter: React.FC<DialogFooterProps>;
export type { DialogOpenEvent, DialogCloseEvent, DialogCancelEvent } from '../core/Dialog';
//# sourceMappingURL=ReactDialog.d.ts.map