import { EventName } from '@lit/react';
import { Pagination, PaginationProps, PageChangeEvent } from '../core/Pagination';
import * as React from 'react';
/**
 * React-specific props interface for Pagination component
 * Extends core PaginationProps with React-specific additions
 */
export interface ReactPaginationProps extends PaginationProps {
    children?: React.ReactNode;
    className?: string;
    id?: string;
}
export declare const ReactPagination: import('@lit/react').ReactWebComponent<Pagination, {
    onPageChange: EventName<PageChangeEvent>;
}>;
export type { PageChangeEvent, PageChangeEventDetail } from '../core/Pagination';
//# sourceMappingURL=ReactPagination.d.ts.map