import { EventName } from '@lit/react';
import { AgToggle, ToggleProps, ToggleChangeEvent } from '../core/Toggle';
import * as React from 'react';
/**
 * React-specific props interface derived from core ToggleProps
 * Extends core props including event handlers
 */
export interface ReactToggleProps extends ToggleProps {
    children?: React.ReactNode;
    className?: string;
    id?: string;
    onClick?: (event: MouseEvent) => void;
    onToggleChange?: (event: ToggleChangeEvent) => void;
}
export declare const ReactToggle: import('@lit/react').ReactWebComponent<AgToggle, {
    onToggleChange: EventName<ToggleChangeEvent>;
}>;
export type { ToggleChangeEvent } from '../core/Toggle';
//# sourceMappingURL=ReactToggle.d.ts.map