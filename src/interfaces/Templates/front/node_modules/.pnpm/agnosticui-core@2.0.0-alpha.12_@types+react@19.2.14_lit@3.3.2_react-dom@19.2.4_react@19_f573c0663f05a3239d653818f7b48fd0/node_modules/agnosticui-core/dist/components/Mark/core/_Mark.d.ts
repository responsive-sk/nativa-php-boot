import { LitElement, TemplateResult } from 'lit';

export type MarkVariant = 'warning' | 'info' | 'success' | 'error' | 'primary' | 'secondary' | 'monochrome';
export interface MarkProps {
    variant?: MarkVariant;
    search?: string;
    caseSensitive?: boolean;
    matchAll?: boolean;
}
/**
 * @element ag-mark
 * @csspart ag-mark - The root wrapping mark element in static mode.
 */
export declare class Mark extends LitElement implements MarkProps {
    static styles: import('lit').CSSResult;
    variant: MarkVariant;
    search?: string;
    caseSensitive: boolean;
    matchAll: boolean;
    private _slot;
    private get _textContent();
    constructor();
    private _onSlotChange;
    private _renderStatic;
    private _renderReactive;
    render(): TemplateResult<1>;
}
//# sourceMappingURL=_Mark.d.ts.map