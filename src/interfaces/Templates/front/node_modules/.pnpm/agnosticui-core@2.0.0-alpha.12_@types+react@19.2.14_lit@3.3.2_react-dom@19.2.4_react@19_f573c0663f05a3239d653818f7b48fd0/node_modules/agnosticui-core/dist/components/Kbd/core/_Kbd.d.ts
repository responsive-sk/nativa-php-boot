import { LitElement } from 'lit';

export type KbdVariant = 'default' | 'primary' | 'secondary' | 'info' | 'warning' | 'success' | 'danger' | 'error' | 'monochrome';
export type KbdSize = 'sm' | 'md' | 'lg';
export interface KbdProps {
    variant?: KbdVariant;
    bordered?: boolean;
    background?: boolean;
    size?: KbdSize;
}
export declare class AgKbd extends LitElement implements KbdProps {
    variant: KbdVariant;
    bordered: boolean;
    background: boolean;
    size: KbdSize;
    constructor();
    static styles: import('lit').CSSResult;
    updated(changedProperties: Map<string, unknown>): void;
    private _checkSingleChar;
    private _onSlotChange;
    render(): import('lit').TemplateResult<1>;
}
//# sourceMappingURL=_Kbd.d.ts.map