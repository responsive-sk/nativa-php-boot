import * as o from "react";
import { o as e } from "../../../create-component-BPMDMe-q.js";
import "../core/Combobox.js";
import { AgCombobox as t } from "../core/_Combobox.js";
const c = e({
  tagName: "ag-combobox",
  elementClass: t,
  react: o,
  events: {
    // Map custom events to React event handlers
    onChange: "change",
    onSelect: "select",
    onSearch: "search",
    onOpen: "open",
    onClose: "close"
    // Native events (focus, blur) work automatically, don't need mapping
  }
});
export {
  c as ReactCombobox
};
