/* Home Page Entry Point
 * Imports: home page-specific styles only
 */

import "../use-cases/home/home.css";

// Dummy export to force Vite to generate CSS bundle
export const homeVersion = '1.0.0';
